<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover, user-scalable=no" />
<meta name="theme-color" content="#05090d" />
<title>PHAGE LAB // 病毒战争复刻版</title>
<style>
  :root {
    --cyan:#19e9e6;
    --magenta:#ff3bd4;
    --lime:#d8ff27;
    --orange:#ff8f2d;
    --ink:#04070a;
    --panel:rgba(10,18,24,.92);
    --line:rgba(142,234,255,.2);
    --text:#d9faff;
    --muted:#71929a;
    --danger:#ff5470;
    --safe:#59f7a2;
  }
  *{box-sizing:border-box;-webkit-tap-highlight-color:transparent}
  html,body{margin:0;width:100%;height:100%;overflow:hidden;background:#020407;color:var(--text);font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,"Liberation Mono",monospace;touch-action:none}
  body::before{content:"";position:fixed;inset:0;pointer-events:none;z-index:50;background:repeating-linear-gradient(0deg,rgba(255,255,255,.018),rgba(255,255,255,.018) 1px,transparent 1px,transparent 4px);mix-blend-mode:screen;opacity:.32}
  button{font:inherit;color:inherit}
  #app{position:fixed;inset:0;background:radial-gradient(circle at 50% 40%,#10202b 0,#050a0e 42%,#010203 100%)}
  .screen{position:absolute;inset:0;display:none}
  .screen.active{display:flex}

  /* boot */
  #boot{align-items:center;justify-content:center;flex-direction:column;gap:18px;padding:26px;text-align:center}
  .logo{font-size:clamp(34px,9vw,88px);letter-spacing:.12em;font-weight:900;color:#eaffff;text-shadow:0 0 12px #4ff,0 0 42px rgba(25,233,230,.45)}
  .logo small{display:block;font-size:.23em;letter-spacing:.62em;color:var(--cyan);margin-top:12px;padding-left:.62em}
  .bootline{width:min(580px,88vw);height:2px;background:#1e3037;overflow:hidden;box-shadow:0 0 14px rgba(25,233,230,.22)}
  .bootline span{display:block;height:100%;width:0;background:linear-gradient(90deg,transparent,var(--cyan),white);animation:load 1.5s ease forwards}
  @keyframes load{to{width:100%}}
  .terminal{font-size:12px;color:#6aa4a8;line-height:1.65;min-height:82px}
  .primary{border:1px solid rgba(25,233,230,.65);background:rgba(6,28,33,.82);padding:13px 24px;min-width:210px;border-radius:3px;letter-spacing:.18em;box-shadow:inset 0 0 24px rgba(25,233,230,.08),0 0 18px rgba(25,233,230,.12);cursor:pointer}
  .primary:active{transform:translateY(1px);background:rgba(25,233,230,.13)}

  /* lab menu */
  #lab{flex-direction:column;padding:max(16px,env(safe-area-inset-top)) max(14px,env(safe-area-inset-right)) max(14px,env(safe-area-inset-bottom)) max(14px,env(safe-area-inset-left));background:linear-gradient(135deg,#050a0d 0,#11191c 48%,#05080a 100%)}
  .topbar{display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);padding:8px 4px 12px;gap:12px}
  .brand{font-size:clamp(16px,4vw,26px);font-weight:800;letter-spacing:.16em}.brand b{color:var(--cyan)}
  .operator{font-size:11px;color:var(--muted);text-align:right}
  .lab-grid{flex:1;min-height:0;display:grid;grid-template-columns:minmax(230px,.78fr) minmax(320px,1.6fr);gap:14px;padding-top:14px}
  .menu-panel,.monitor{border:1px solid rgba(142,234,255,.18);background:rgba(3,8,11,.76);box-shadow:inset 0 0 45px rgba(21,105,115,.05);min-height:0}
  .menu-panel{display:flex;flex-direction:column;padding:14px;gap:10px}
  .menu-btn{border:1px solid rgba(142,234,255,.22);background:linear-gradient(90deg,rgba(8,26,31,.92),rgba(6,11,14,.88));padding:15px 14px;text-align:left;cursor:pointer;position:relative;overflow:hidden}
  .menu-btn::after{content:"";position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--cyan);opacity:.35}
  .menu-btn strong{display:block;font-size:15px;letter-spacing:.12em}.menu-btn span{display:block;font-size:10px;color:#72939c;margin-top:5px;line-height:1.45}
  .menu-btn:hover,.menu-btn:active{border-color:rgba(25,233,230,.6);background:rgba(12,42,48,.78)}
  .menu-btn.danger::after{background:var(--danger)}
  .lab-stats{margin-top:auto;border-top:1px solid var(--line);padding-top:12px;font-size:11px;color:#6f979f;line-height:1.8}
  .monitor{position:relative;overflow:hidden;display:flex;align-items:center;justify-content:center;background:radial-gradient(circle at 48% 46%,rgba(23,79,83,.24),rgba(1,4,6,.94) 64%)}
  #virusPreview{width:100%;height:100%;display:block}
  .monitor-label{position:absolute;left:16px;top:14px;font-size:10px;color:#73979e;letter-spacing:.15em}
  .monitor-data{position:absolute;right:14px;bottom:12px;text-align:right;font-size:10px;color:#608890;line-height:1.7}

  /* experiment selection */
  #experiments,#sequencer,#help{flex-direction:column;background:linear-gradient(145deg,#050a0d,#0d151a);padding:max(12px,env(safe-area-inset-top)) max(12px,env(safe-area-inset-right)) max(12px,env(safe-area-inset-bottom)) max(12px,env(safe-area-inset-left))}
  .subhead{display:flex;align-items:center;gap:12px;border-bottom:1px solid var(--line);padding:4px 0 12px}
  .back{width:42px;height:38px;border:1px solid rgba(142,234,255,.25);background:#071014;cursor:pointer}
  .subhead h2{font-size:16px;letter-spacing:.15em;margin:0;flex:1}.subhead .meta{font-size:10px;color:var(--muted)}
  #levelGrid{flex:1;overflow:auto;display:grid;grid-template-columns:repeat(8,minmax(72px,1fr));grid-auto-rows:minmax(76px,1fr);gap:9px;padding:14px 2px 4px;touch-action:pan-y}
  .level{position:relative;border:1px solid rgba(142,234,255,.2);background:radial-gradient(circle,rgba(22,59,65,.3),rgba(3,8,10,.92));min-height:76px;cursor:pointer;overflow:hidden}
  .level .n{position:absolute;left:9px;top:8px;font-weight:800;font-size:16px}.level .status{position:absolute;left:9px;bottom:8px;font-size:9px;color:#64858d}.level .dot{position:absolute;right:10px;top:10px;width:8px;height:8px;border-radius:50%;background:#34484e;box-shadow:0 0 8px currentColor}
  .level.done{border-color:rgba(89,247,162,.42)}.level.done .dot{background:var(--safe);color:var(--safe)}
  .level.current{border-color:rgba(25,233,230,.72);box-shadow:inset 0 0 24px rgba(25,233,230,.08)}.level.current .dot{background:var(--cyan);color:var(--cyan)}
  .level.locked{opacity:.32;filter:grayscale(1);cursor:not-allowed}.level:active:not(.locked){transform:scale(.985)}

  /* sequencer */
  .sequence-layout{flex:1;min-height:0;display:grid;grid-template-columns:minmax(245px,.75fr) minmax(350px,1.45fr);gap:12px;padding-top:12px}
  .stat-card,.gene-card{border:1px solid rgba(142,234,255,.18);background:rgba(4,10,13,.8);padding:14px;min-height:0}
  .stat-card{display:flex;flex-direction:column}.dna-visual{height:190px;position:relative;overflow:hidden;border-bottom:1px solid var(--line);margin-bottom:12px}
  #dnaCanvas{width:100%;height:100%}
  .capacity{font-size:11px;color:#7ba0a7;margin:8px 0 12px}.capacity-bar{height:8px;border:1px solid rgba(142,234,255,.25);margin-top:6px;padding:1px}.capacity-bar span{display:block;height:100%;background:linear-gradient(90deg,var(--cyan),var(--magenta));width:0}
  .stats-list{display:grid;gap:9px}.stat-row{display:grid;grid-template-columns:95px 1fr 44px;align-items:center;gap:8px;font-size:11px}.meter{height:5px;background:#112127}.meter span{display:block;height:100%;background:var(--cyan)}.stat-value{text-align:right;color:#a8e8ef}
  #geneList{height:100%;overflow:auto;display:grid;grid-template-columns:repeat(3,1fr);gap:8px;align-content:start;touch-action:pan-y}
  .gene{border:1px solid rgba(142,234,255,.18);background:#071015;padding:10px 9px;min-height:86px;cursor:pointer;position:relative}.gene.locked{opacity:.28;cursor:not-allowed}.gene.active{border-color:rgba(255,59,212,.8);box-shadow:inset 0 0 22px rgba(255,59,212,.09)}
  .gene strong{font-size:12px;letter-spacing:.14em;color:#dffcff}.gene .size{position:absolute;right:8px;top:8px;color:var(--magenta);font-size:10px}.gene p{font-size:9px;color:#6e9198;line-height:1.5;margin:8px 0 0}

  /* help */
  .help-body{flex:1;overflow:auto;touch-action:pan-y;padding:16px 4px;max-width:900px;width:100%;margin:auto;line-height:1.75;color:#a8c7cd;font-size:13px}.help-body h3{color:#eaffff;letter-spacing:.1em}.help-body kbd{border:1px solid #426069;padding:2px 5px;color:var(--cyan);background:#071014}.help-body .notice{border-left:3px solid var(--cyan);padding:10px 13px;background:rgba(25,233,230,.05);color:#8db0b7}

  /* game */
  #game{background:#010204;display:none}.screen#game.active{display:block}
  #gameCanvas{position:absolute;inset:0;width:100%;height:100%;display:block;touch-action:none}
  .game-hud{position:absolute;left:max(9px,env(safe-area-inset-left));right:max(9px,env(safe-area-inset-right));top:max(8px,env(safe-area-inset-top));display:flex;align-items:flex-start;justify-content:space-between;pointer-events:none;z-index:5}
  .hud-box{border:1px solid rgba(184,241,255,.2);background:rgba(2,6,8,.68);backdrop-filter:blur(5px);padding:8px 10px;font-size:10px;color:#7399a1;line-height:1.55;pointer-events:auto}.hud-box b{color:#dffcff;font-size:12px}.hud-actions{display:flex;gap:6px;pointer-events:auto}.icon-btn{border:1px solid rgba(184,241,255,.25);background:rgba(2,7,9,.75);width:42px;height:40px;cursor:pointer;font-size:17px}
  .game-tip{position:absolute;left:50%;bottom:max(14px,env(safe-area-inset-bottom));transform:translateX(-50%);background:rgba(2,7,9,.76);border:1px solid rgba(142,234,255,.18);font-size:10px;color:#82a3aa;padding:8px 12px;white-space:nowrap;pointer-events:none;transition:opacity .3s;z-index:4}
  #toast{position:absolute;left:50%;top:15%;transform:translate(-50%,-20px);opacity:0;z-index:12;background:rgba(5,12,15,.93);border:1px solid rgba(25,233,230,.55);padding:11px 18px;font-size:12px;letter-spacing:.12em;transition:.25s;pointer-events:none}#toast.show{opacity:1;transform:translate(-50%,0)}
  .modal{position:absolute;inset:0;display:none;align-items:center;justify-content:center;background:rgba(0,3,5,.82);z-index:20;padding:20px}.modal.show{display:flex}.modal-card{width:min(460px,94vw);border:1px solid rgba(142,234,255,.28);background:linear-gradient(145deg,#071116,#020507);pad