# 《Age of War（战争进化史）》复刻提示词

> 使用方法：把下面「===== 提示词开始 =====」到「---

# 九、已知特性（不是 bug，不用去"修"）

这个玩法有一个固有性质：**双方水平完全对等时，战线会稳定卡在中间，谁也推不动**。
因为只有最前排的近战 + 射程够得着的远程能输出，后排单位纯粹排队，所以「钱更多」并不能直接换成战线推进；
两个策略一模一样的对手会打成长期拉锯。实测中，只要一方打法有偏差（只造便宜兵、只造远程、囤钱不出兵），
3~10 分钟内就会分出胜负。所以不要为了"让 AI 对打能结束"去魔改机制——对人类玩家是正常的。

===== 提示词结束 =====」之间的全部内容，
> 原样发给 AI（Claude / ChatGPT / Gemini 均可）。数值表已经写成 JS 常量，AI 可以直接抄进代码。
> 文末附「迭代追问模板」，用于第一版出来后继续打磨手感。

---

===== 提示词开始 =====

# 角色

你是一名资深 2D 网页游戏开发工程师，擅长用原生 Canvas 实现手感精准的复刻作品。

# 任务

复刻 2007 年 Louissi 的 Flash 经典《Age of War》（中文名《战争进化史》），目标是**玩起来和原版几乎一样**：
玩法规则、操作流程、UI 布局、节奏与数值曲线都要还原。美术不要求一致（**禁止使用原版任何素材**），
全部用 Canvas 代码绘制的原创图形表现即可。

# 交付物

- **一个 `index.html` 单文件**：CSS 与 JS 全部内联，Canvas 2D 渲染，**零外部依赖**（不许 CDN、不许外部图片/字体/音频），双击即可运行。
- 代码结构要求：把 `AGES / UNITS / TURRETS / SPECIALS / BALANCE` 等**数值常量表集中放在文件顶部**，与逻辑代码彻底分离，方便我事后调参。
- 主循环用**固定时间步长**（逻辑步长 1/60 秒，用累加器驱动，渲染插值可选），保证不同刷新率下手感一致。
- 逻辑分辨率 1280×720，等比缩放铺满窗口并保持黑边（letterbox）；支持鼠标与触摸。

---

# 一、画面布局（照抄原版的信息结构）

- 侧视图单屏战场，**没有摄像机移动**，两个基地同时可见。地面是 y≈560 的一条水平线。
- 玩家基地在 x≈90（左侧），敌方基地在 x≈1190（右侧）。玩家单位向右走，敌方单位向左走。
- 顶部 HUD：
  - 左：玩家基地血条（显示数值）、金币数、经验条（到下一时代的进度）
  - 中：当前时代名称
  - 右：敌方基地血条
- 底部控制栏（原版布局，必须还原）：
  - 左侧 3 个**兵种按钮**（当前时代的 3 个兵种）：图标 + 名称 + 价格，钱不够时置灰
  - 中间**生产队列**格子（上限 6 格），第一格带倒计时进度条
  - **进化按钮**：经验未满时置灰并显示还差多少，满了高亮闪烁
  - **炮塔按钮**：打开面板，可购买炮塔位 / 在空位建造当前时代炮塔 / 拆除炮塔
  - **必杀技按钮**：显示剩余次数与充能进度环
- 所有按钮悬停显示 tooltip（价格、攻击、血量、射程、简短说明）。
- 快捷键：`1/2/3` 出兵，`4` 进化，`5` 必杀，`Q/W/E/R` 对应 4 个炮塔位，`空格` 暂停。

---

# 二、核心循环（这几条决定"像不像原版"，务必严格实现）

1. **金币只来自击杀敌方单位**——没有被动收入、没有采集、没有时间给钱。这是原版的经济核心，不要擅自加"每秒 +N 金币"。
2. **经验也只来自击杀**，攒够经验才能进化。
3. 单位**自动前进、自动攻击**，玩家唯一的操作是「造什么兵 / 何时进化 / 造不造炮塔 / 何时放必杀」。
4. 一方基地血量归零即结束。

## 2.1 单位行为（还原手感的关键）

- 单位在本方基地出生点生成后，**沿地面单向匀速前进，永不后退、不走位、不寻路**（原版就是纯排队推进，不要加 A\*、不要加绕行）。
- **一维列队**：同阵营单位**不能互相穿过**。后面的单位撞到前面的就停下（保持最小间距 ≈28px）。这条是原版"堵成一排"手感的来源。
- 攻击判定：每个单位有 `range`。当**最近的敌方单位或敌方基地**进入 `range` 时，立即停止移动并进入攻击循环，每 `atkInterval` 秒造成一次 `dmg`。目标死亡后重新索敌；无目标则继续前进。
- 远程单位发射**可见弹丸**（箭/子弹/炮弹，直线或小抛物线），伤害在弹丸命中时结算（不是发射瞬间）。
- 单位死亡时，**击杀方立刻获得该单位的 `goldReward` 和 `xpReward`**——无论击杀者是士兵、炮塔还是必杀技。
- 走到敌方基地前就攻击基地（基地视作射程内的目标）。
- **攻城规则（必须实现，否则基地永远掉不了血）**：当单位与敌方基地正面的距离 ≤ 60px 时，
  **直接攻击基地，不再去追新刷出来的挡路兵**。否则防守方只要不停刷最便宜的兵贴脸挡路，
  进攻方就会无限追杀这些挡路兵，基地一滴血都掉不了，对局永远打不完。

## 2.2 生产队列

- 点击兵种按钮：**立即扣钱**并入队，队列上限 6；钱不够则按钮无响应（可播放"叮"的错误提示）。
- 队列第一位按 `buildTime` 倒计时，完成后在出生点生成，然后处理下一个。
- **堵门规则**：若出生点前方 30px 内已有己方单位，出兵暂停（倒计时保持完成状态）直到空出。原版存在这种堵门现象，请保留。
- 入队后不可取消、不退款。
- 进化后，队列里已下单的单位仍按下单时的兵种生成。

## 2.3 进化

- 经验达到当前时代阈值后可进化。点击进化：进入下一时代，**解锁新的 3 个兵种、新炮塔、新必杀技**；经验条清零并换成下一档阈值。
- **已在场的单位不变**；**已建造的炮塔不自动升级**（想换代得先拆再建）。
- 基地外观随时代改变（用不同几何造型与配色表现：石堆 → 城堡 → 石砌堡垒 → 混凝土碉堡 → 未来合金要塞）。
- 进化不花金币，播放 1 秒全屏闪光 + 基地变形动画。
- 第 5 时代是最终形态，进化按钮变成"已达最高时代"。

## 2.4 炮塔

- 基地上最多 **4 个炮塔位**，需要**先花钱买位置**，价格递增；买完位置后再花钱在该位置建造**当前时代的炮塔**（每个时代只有 1 种炮塔）。
- 炮塔自动攻击射程内**最靠近己方基地的敌人**（优先拦截最深入的），有射击间隔与可见弹道。
- 炮塔可拆除，返还建造费的 50%（炮塔位不退款）。
- 炮塔不可被攻击、不会被摧毁（原版如此），但敌方 AI 同样能买炮塔位和炮塔。

## 2.5 必杀技

- 每个时代 1 个专属必杀，**全屏 AOE**：对场上**所有**敌方单位造成固定伤害，**不伤基地**。
- 充能：上限 3 次，开局 1 次，每 `50 秒` 自动 +1；**每次进化立即回满**。
- 释放时播放约 1.5 秒的全屏演出（落石 / 箭雨 / 炮击 / 空袭 / 轨道激光）+ 屏幕震动，期间游戏不暂停。

## 2.6 胜负

- 任一基地 HP ≤ 0 立即结束，弹出结算面板：胜利/失败、用时、击杀数、总收入、到达时代，提供「再来一局」。
- 基地血量**不会自然恢复**。

---

# 三、数值表（直接用作 JS 常量，请原样实现）

```js
const BALANCE = {
  baseHp: 1500,          // 双方基地血量，全程不变（实测 4000 会导致对局永远打不完）
  laneY: 560,            // 地面高度
  playerBaseX: 90,
  enemyBaseX: 1190,
  unitGap: 28,           // 同阵营单位最小间距
  queueMax: 6,
  turretSlotCosts: [600, 1800, 4500, 10000], // 第 1~4 个炮塔位价格
  turretRefund: 0.5,
  specialMaxCharges: 3,
  specialRechargeSec: 50,
  startGold: 200,
};

// 进化所需经验（进化后经验清零，逐档累计）
const AGES = [
  { id: 1, name: '石器时代',   xpToNext: 1000  },
  { id: 2, name: '城堡时代',   xpToNext: 2500  },
  { id: 3, name: '文艺复兴',   xpToNext: 6000  },
  { id: 4, name: '现代',       xpToNext: 11000 },
  { id: 5, name: '未来时代',   xpToNext: null  }, // 最终时代
];

// 每个时代固定 3 个兵种：便宜近战 / 中价远程 / 昂贵重型
// range<40 视为近战；speed 单位 px/s；atkInterval 单位秒
const UNITS = {
  1: [
    { key:'clubman',   name:'棍棒人',   cost:25,   hp:55,   dmg:12,  atkInterval:1.0, range:22,  speed:32, buildTime:1.0, gold:30,   xp:20   },
    { key:'slinger',   name:'投石人',   cost:50,   hp:45,   dmg:14,  atkInterval:1.4, range:140, speed:28, buildTime:2.0, gold:65,   xp:40   },
    { key:'dinorider', name:'恐龙骑士', cost:120,  hp:200,  dmg:32,  atkInterval:1.3, range:28,  speed:24, buildTime:4.5, gold:155,   xp:110  },
  ],
  2: [
    { key:'swordsman', name:'剑士',     cost:60,   hp:130,  dmg:26,  atkInterval:0.9, range:24,  speed:34, buildTime:1.4, gold:80,   xp:55   },
    { key:'archer',    name:'弓箭手',   cost:120,  hp:100,  dmg:30,  atkInterval:1.3, range:170, speed:30, buildTime:2.4, gold:155,   xp:100  },
    { key:'knight',    name:'骑士',     cost:260,  hp:420,  dmg:65,  atkInterval:1.2, range:30,  speed:26, buildTime:5.0, gold:340,  xp:240  },
  ],
  3: [
    { key:'pikeman',   name:'长矛兵',   cost:140,  hp:300,  dmg:55,  atkInterval:0.9, range:26,  speed:34, buildTime:1.6, gold:180,   xp:120  },
    { key:'musketeer', name:'火枪手',   cost:280,  hp:230,  dmg:70,  atkInterval:1.5, range:200, speed:30, buildTime:2.6, gold:365,  xp:230  },
    { key:'cannon',    name:'加农炮',   cost:600,  hp:900,  dmg:150, atkInterval:2.0, range:230, speed:20, buildTime:5.5, gold:780,  xp:520  },
  ],
  4: [
    { key:'rifleman',  name:'突击兵',   cost:320,  hp:700,  dmg:120, atkInterval:0.7, range:70,  speed:36, buildTime:1.8, gold:415,  xp:260  },
    { key:'bazooka',   name:'火箭筒兵', cost:620,  hp:520,  dmg:170, atkInterval:1.6, range:230, speed:30, buildTime:2.8, gold:805,  xp:500  },
    { key:'tank',      name:'坦克',     cost:1300, hp:2000, dmg:330, atkInterval:1.8, range:210, speed:22, buildTime:6.0, gold:1690,  xp:1100 },
  ],
  5: [
    { key:'laser',     name:'激光兵',   cost:700,  hp:1500, dmg:260, atkInterval:0.7, range:90,  speed:38, buildTime:2.0, gold:910,  xp:560  },
    { key:'plasma',    name:'等离子炮手',cost:1300,hp:1100, dmg:380, atkInterval:1.5, range:250, speed:32, buildTime:3.0, gold:1690,  xp:1100 },
    { key:'mech',      name:'战斗机甲', cost:2600, hp:4500, dmg:700, atkInterval:1.7, range:220, speed:22, buildTime:6.5, gold:3380, xp:2400 },
  ],
};

// 每个时代 1 种炮塔
const TURRETS = {
  1: { name:'投石台',   cost:200,  dmg:22,  atkInterval:1.6, range:260 },
  2: { name:'弩炮塔',   cost:500,  dmg:45,  atkInterval:1.4, range:300 },
  3: { name:'加农炮台', cost:1200, dmg:110, atkInterval:1.8, range:340 },
  4: { name:'导弹塔',   cost:2800, dmg:240, atkInterval:1.5, range:380 },
  5: { name:'激光炮台', cost:6000, dmg:520, atkInterval:1.3, range:420 },
};

// 每个时代 1 个必杀技：对全场敌方单位造成固定伤害，不伤基地
const SPECIALS = {
  1: { name:'落石雨',   dmg:120  },
  2: { name:'箭雨',     dmg:260  },
  3: { name:'火炮齐射', dmg:550  },
  4: { name:'空中打击', dmg:1200 },
  5: { name:'轨道激光', dmg:2600 },
};
```

**数值说明**：以上数值经过实机模拟配平（AI 自动对打上百局验证），请原样实现。三条最关键的约束：

- **击杀金币 ≈ 单位造价 × 1.3**。这条决定整局经济能不能转起来。低于 1.0 时，1 换 1 的交换会持续净亏钱，
  双方会在 1 分钟内同时破产、永远卡在石器时代；等于 1.0 时全场金币守恒（总共就那点本金），
  永远攒不出后期的重型兵和炮塔。1.3 才能既维持出兵、又让局势随时代升级。
- **基地血量 1500**。看着低，但防守方在自家门口优势极大（新兵贴着基地刷出来挡路），
  血量给到 2500 以上时对局基本永远打不完。
- **经验阈值 1000/2500/6000/11000**，对应每个时代约 2~3 分钟、一局 8~20 分钟打满 5 个时代。

---

# 四、敌方 AI

- **敌方使用与玩家完全相同的规则**：同样从击杀获得金币/经验、同样的队列、同样的炮塔位与必杀技，不作弊、不凭空刷钱（难度只通过下面的倍率体现）。
- 倍率**同时作用于金币和经验**：只加金币是没用的——后排单位打不到人，钱多并不能转化成战线压力，
  必须让高难度 AI 在**时代进度**上压制玩家，难度梯度才真的能被感觉到。
- 决策循环：每 `reactionSec` 秒评估一次
  1. 能进化就**立刻进化**；
  2. **先保证前排有近战顶着**：如果场上没有单位、或最前面的单位射程 ≥ 100，就先买 1 号兵种（射程最短的那个）。
     这条很重要——远程单位站在队首会按自己的射程停下，把后面的己方近战全堵在射程之外，
     双方就会隔着 200px 干瞪眼对射，谁也推不动；
  3. 场上己方单位 < 3 或队列里不足 2 个 → 出兵：70% 概率买**买得起的最贵兵种**，30% 概率买最便宜的凑人数；
     （只在"队列为空"时才补兵会导致 AI 后期囤几万金币不花）
  4. 若距离进化只差一次击杀左右，倾向存钱等重型兵；
  5. 金币 > `炮塔位价格 + 炮塔价格 + 500` 时，买炮塔位并建塔（优先补满已有空位）；
  6. 己方基地 400px 内敌方单位 ≥ 4，或基地血量 < 40% 且有充能时，放必杀。
- **难度 1~5**（开局菜单选择）：
  | 难度 | 击杀金币与经验倍率 | reactionSec | 备注 |
  |---|---|---|---|
  | 1 简单 | 0.8 | 1.2 | 不建炮塔 |
  | 2 普通 | 1.0 | 0.9 | 最多 1 座炮塔 |
  | 3 困难 | 1.15 | 0.7 | 最多 2 座 |
  | 4 专家 | 1.3 | 0.5 | 最多 3 座 |
  | 5 噩梦 | 1.5 | 0.4 | 满 4 座，必杀更激进 |
- 另做一个「战役」入口：连打 5 关，难度从 1 递增到 5（对应原版逐关变难的流程）。

---

# 五、美术与表现（原创，简洁但不敷衍）

- 全部用 Canvas 图元绘制：单位是**带描边的剪影式小人/载具**（约 34~60px 高），不同时代配色不同，
  玩家方偏冷色（蓝/青），敌方偏暖色（红/橙），保证一眼能分清阵营。
- 单位要有简单的**行走动画**（两帧腿部摆动或上下起伏）和**攻击动作**（挥手/后坐力）。
- 单位头顶血条只在受伤后显示。
- 表现细节：命中火花粒子、伤害飘字、死亡后倒地并淡出 0.5 秒、进化白光、必杀屏幕震动。
- 背景：远景山脉/建筑天际线随时代变化（石器：荒原与巨石；城堡：城墙；文艺复兴：塔楼；现代：城市；未来：霓虹都市），
  用纯色块 + 简单几何绘制即可，**不要加载任何外部图片**。
- 音效可选：用 WebAudio 合成短促方波/噪声（打击、出兵、进化、必杀），并提供静音按钮。

---

# 六、禁止事项

- ❌ 不得使用原版的任何美术、音频、字体素材（版权原因），全部原创绘制。
- ❌ 不得引入任何外部库或 CDN（无 Phaser / PixiJS / jQuery / 外链字体）。
- ❌ 不得添加原版没有的机制：英雄单位、技能树、装备、随机掉落、抽卡、多路兵线、被动金币收入。
- ❌ 不得改成 3D、不得引入物理引擎、不得加寻路算法。
- ❌ 不要把逻辑写死在渲染里——数值表必须可独立修改。

---

# 七、完成前自检清单（请逐条核对后再输出）

- [ ] 单文件 `index.html` 打开即玩，控制台无报错
- [ ] 5 个时代 × 3 兵种 = 15 个兵种全部可用，数值与上表一致
- [ ] 金币和经验**只**从击杀产生，且击杀金币 ≈ 造价 ×1.3（拿一局跑 3 分钟，双方金币不能双双归零）
- [ ] 单位顶到敌方基地 60px 内会直接砸基地，不会被源源不断的挡路兵无限拖住
- [ ] AI 会先出近战顶前排，不会让远程站队首把自己人堵在射程外
- [ ] 同阵营单位不会互相穿模，会排队堵住
- [ ] 远程单位有可见弹丸，伤害在命中时结算
- [ ] 生产队列上限 6，第一格有进度条，出生点被堵时会暂停出兵
- [ ] 进化后新兵种/新炮塔/新必杀全部生效，场上老单位与老炮塔不变
- [ ] 4 个炮塔位可购买、可建造、可拆除并返还 50%
- [ ] 必杀技全屏 AOE、有次数上限与充能、进化回满、不伤基地
- [ ] 敌方 AI 会出兵、会进化、会建炮塔、会放必杀，且遵守与玩家相同的经济规则
- [ ] 有难度选择与 5 关战役
- [ ] 有胜负结算面板和重开按钮
- [ ] 窗口缩放不变形，键盘快捷键可用

# 八、输出格式

先输出**完整的 `index.html` 代码**（一次性给全，不要省略成 `// ...`），代码后再用不超过 10 行说明：
数值表在第几行、怎么调难度、怎么加新时代。

---

# 九、已知特性（不是 bug，不用去"修"）

这个玩法有一个固有性质：**双方水平完全对等时，战线会稳定卡在中间，谁也推不动**。
因为只有最前排的近战 + 射程够得着的远程能输出，后排单位纯粹排队，所以「钱更多」并不能直接换成战线推进；
两个策略一模一样的对手会打成长期拉锯。实测中，只要一方打法有偏差（只造便宜兵、只造远程、囤钱不出兵），
3~10 分钟内就会分出胜负。所以不要为了"让 AI 对打能结束"去魔改机制——对人类玩家是正常的。

===== 提示词结束 =====

---

## 迭代追问模板（第一版出来后按需发给 AI）

1. **手感不对**：「战斗节奏太快/太慢，请把所有单位的 `atkInterval` ×1.3、`speed` ×0.85，其余不变，只输出改动后的数值表。」
2. **经济太紧**：「前期攒不出钱，请把 1~2 时代的 `gold` 奖励 ×1.25，`xpToNext` 不变。」
3. **AI 太弱/太强**：「难度 3 的 AI 总是被压制，请只改 AI 决策逻辑：让它在场上劣势时优先出远程兵并保留必杀到 6 个敌人时再放。」
4. **加手机支持**：「请增加移动端适配：底部按钮改为不小于 44px 的触摸区域，横屏提示，取消 hover tooltip 改为长按显示。」
5. **加存档**：「用 localStorage 保存战役进度、最高通关难度和设置项。」
6. **换引擎**：把提示词里的「交付物」一节替换为你要的技术栈（Unity / Godot / React + TS），其余章节可原样复用。
